{{#if updateStory}}
<h4>Update Story</h4>
<div>
  <form>
    <div class="form-group">
      <label for="title">Title</label>
      {{input class="form-control" value=story.title id="title"}}
    </div>

    <div class="form-group">
      <label for="author">Author</label>
      {{input class="form-control" value=story.author id="author"}}
    </div>

    <div class="form-group">
      <label for="description">Short Description</label>
      {{input class="form-control" value=story.description id="description"}}
    </div>

    <div class="form-group">
      <label for="image">Image URL</label>
      {{input class="form-control" value=story.image id="image"}}
    </div>

    <div class="form-group">
      <label for="date">Date</label>
      {{input class="form-control" value=story.date id="date"}}
    </div>

    <div class="form-group">
      <label for="article">Full Text</label>
      {{input class="form-control" value=story.text id="article"}}
    </div>

    <button class="btn btn-primary" {{action 'update' story}}>Save</button>
  </form>
</div>
{{else}}
  <button class="btn btn-primary" {{action 'updateStoryFormShow'}}>Update Story</button>
{{/if}}
